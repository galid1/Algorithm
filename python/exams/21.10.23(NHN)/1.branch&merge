# 문제
1. branch 명령어 입력 시 가능한 가지 목록(2...100000) 중 가장 작은 수를 가져와 가지에 더한다.
2. merge x 명령어 입력 시 가지에 합쳐버리고, x 수를 가지 목록에 반환한다

# 풀이
1. 현재 가지에 추가된 목록을 Set으로 유지한다.
2. 가능한 가지 목록은 Heap으로 유지한다.
3. branch 명령 시 가능한 가지 목록에서 가장 작은 수를 꺼내와 추가된 목록에 add한다.
4. merge x 입력 시 x에 해당하는 값을 set에서 제거하여, heap에 추가한다.